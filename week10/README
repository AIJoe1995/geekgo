1. 生成随机数模拟结点的负载情况，随机数随时间变化 定义Ticker每个ticker生成一个随机数 还需要一个id标明节点身份
2. 需要把负载情况传到一个中间位置 进行多个结点的负载对比，采用redis zset存储
3. 定时任务续期时（或抢锁时）， 判断自己这个节点在zset里是不是很靠后 负载很大，如果负载大 就不进行续期
4. 选中的节点宕机会在分布式锁过期时自动释放，由其他结点抢到锁